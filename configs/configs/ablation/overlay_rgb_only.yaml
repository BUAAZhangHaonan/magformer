# overlay_rgb_only.yaml
_BASE_: ../mgm_swin_convnext_tiny.yaml
MODEL:
  MGM:
    ENABLED: False
  DPE:
    ENABLED: False

SOLVER:
  IMS_PER_BATCH: 4
  BASE_LR: 5e-5
  STEPS: (18000, 19000)
  MAX_ITER: 20000
  WARMUP_FACTOR: 0.01
  WARMUP_ITERS: 5000

