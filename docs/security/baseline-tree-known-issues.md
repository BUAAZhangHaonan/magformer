# Baseline Tree Known Issues

Vendored baseline code under `baselines/` has not been security-hardened. This file is a read-only audit. Do NOT modify baseline code based on this list — fix issues upstream or document workarounds.

| File path | Line | Exact unsafe call | Risk assessment |
|---|---:|---|---|
| `baselines/Mask2Former/tools/convert-pretrained-swin-model-to-d2.py` | 25 | `obj = torch.load(input, map_location="cpu")["model"]` | Conversion helper loads a checkpoint with pickle-based deserialization; low risk if the source model file is trusted. |
| `baselines/Mask2Former/tools/convert-torchvision-to-d2.py` | 30 | `obj = torch.load(input, map_location="cpu")` | Conversion helper loads a checkpoint with pickle-based deserialization; low risk if the source model file is trusted. |
| `baselines/detectron2/detectron2/checkpoint/detection_checkpoint.py` | 73 | `data = pickle.load(f, encoding="latin1")` | Checkpoint loader deserializes pickle content directly; unsafe if checkpoint files are untrusted. |
| `baselines/detectron2/detectron2/checkpoint/detection_checkpoint.py` | 88 | `data = torch.load(f)` | Legacy Detectron2 checkpoint path uses pickle-backed torch loading; unsafe if checkpoint files are untrusted. |
| `baselines/detectron2/detectron2/data/build.py` | 134 | `proposals = pickle.load(f, encoding="latin1")` | Proposal cache loading uses pickle directly; unsafe if proposal files are untrusted. |
| `baselines/detectron2/detectron2/data/common.py` | 186 | `return pickle.loads(bytes)` | Byte deserialization path will execute pickle reducers; unsafe if bytes originate from an untrusted source. |
| `baselines/detectron2/detectron2/export/caffe2_modeling.py` | 365 | `self.anchor_generator = torch.load(serialized_anchor_generator)` | Export path loads serialized tensors with pickle-backed torch loading; unsafe if the artifact is untrusted. |
| `baselines/detectron2/projects/DensePose/densepose/evaluation/densepose_coco_evaluation.py` | 174 | `arrays = pickle.load(hFile, encoding="latin1")` | DensePose evaluation loads pickled arrays directly; unsafe if cached evaluation data is untrusted. |
| `baselines/detectron2/projects/DensePose/densepose/modeling/cse/embedder.py` | 101 | `state_dict = pickle.load(hFile, encoding="latin1")` | Embedder path deserializes pickled state directly; unsafe if the source file is untrusted. |
| `baselines/detectron2/projects/DensePose/densepose/modeling/cse/embedder.py` | 104 | `state_dict = torch.load(hFile, map_location=torch.device("cpu"))` | DensePose embedder fallback loads a checkpoint without `weights_only=True`; unsafe if the file is untrusted. |
| `baselines/detectron2/projects/DensePose/densepose/modeling/cse/vertex_direct_embedder.py` | 61 | `data = pickle.load(hFile)` | Vertex embedder loads pickle content directly; unsafe if the source file is untrusted. |
| `baselines/detectron2/projects/DensePose/densepose/modeling/cse/vertex_feature_embedder.py` | 72 | `data = pickle.load(hFile)` | Vertex feature embedder loads pickle content directly; unsafe if the source file is untrusted. |
| `baselines/detectron2/projects/DensePose/densepose/structures/mesh.py` | 142 | `return torch.as_tensor(pickle.load(hFile)[field], dtype=torch.float).to(device)` | Mesh structure helper performs inline pickle loading; unsafe if mesh assets are untrusted. |
| `baselines/detectron2/projects/DensePose/densepose/structures/mesh.py` | 151 | `return torch.as_tensor(pickle.load(hFile), dtype=torch.float).to(device)` | Mesh structure helper performs inline pickle loading; unsafe if mesh assets are untrusted. |
| `baselines/detectron2/projects/DensePose/densepose/structures/mesh.py` | 160 | `symmetry_loaded = pickle.load(hFile)` | Mesh symmetry loading uses pickle directly; unsafe if symmetry assets are untrusted. |
| `baselines/detectron2/tests/data/test_dataset.py` | 90 | `ds = pickle.loads(pickle.dumps(ds))` | Test-only pickle roundtrip is low risk in normal CI, but it remains an unsafe pattern if copied into runtime code. |
| `baselines/detectron2/tools/convert-torchvision-to-d2.py` | 35 | `obj = torch.load(input, map_location="cpu")` | Conversion helper loads a checkpoint with pickle-based deserialization; low risk if the source model file is trusted. |
| `baselines/msmformer/lib/fcn/config.py` | 440 | `yaml_cfg = edict(yaml.load(f, Loader=yaml.Loader))` | Config loading uses the unsafe YAML loader, which can construct arbitrary Python objects from untrusted YAML. |
| `baselines/msmformer/lib/fcn/config.py` | 449 | `yaml_cfg = edict(yaml.load(f, Loader=yaml.Loader))` | Config loading uses the unsafe YAML loader, which can construct arbitrary Python objects from untrusted YAML. |
| `baselines/msmformer/lib/fcn/get_backbone.py` | 194 | `network_data = torch.load(args.pretrained)` | Backbone loader reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/lib/fcn/get_backbone.py` | 207 | `network_data_crop = torch.load(args.pretrained_crop)` | Crop backbone loader reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/lib/fcn/get_network_crop.py` | 56 | `network_data = torch.load(pretrained)` | Network crop helper reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/lib/fcn/get_network_crop.py` | 79 | `network_data_crop = torch.load(pretrained_crop)` | Network crop helper reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/tools/get_network.py` | 32 | `network_data = torch.load(pretrained)` | Tooling helper reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/tools/get_network.py` | 83 | `network_data_crop = torch.load(pretrained_crop)` | Tooling helper reads cropped pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/tools/test_images.py` | 190 | `network_data = torch.load(args.pretrained)` | Test image path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/tools/test_images.py` | 203 | `network_data_crop = torch.load(args.pretrained_crop)` | Test image crop path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/tools/test_net.py` | 110 | `network_data = torch.load(args.pretrained)` | Test net path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/tools/test_net.py` | 124 | `network_data_crop = torch.load(args.pretrained_crop)` | Test net crop path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/tools/test_npy.py` | 192 | `network_data = torch.load(args.pretrained)` | Test NPY path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/tools/test_npy.py` | 205 | `network_data_crop = torch.load(args.pretrained_crop)` | Test NPY crop path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/msmformer/tools/train_net.py` | 115 | `network_data = torch.load(args.pretrained)` | Training path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/run_ucn_0831_1k.py` | 375 | `network_data = torch.load(str(pretrained_path))` | UCN runner loads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/run_ucn_ecc.py` | 489 | `network_data = torch.load(str(pretrained_path))` | ECC runner loads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/ultralytics/ultralytics/models/yolo/world/train.py` | 153 | `txt_map = torch.load(cache_path, map_location=self.device)` | Training cache path loads torch artifacts without `weights_only=True`; unsafe if the cache file is untrusted. |
| `baselines/ultralytics/ultralytics/models/yolo/yoloe/train.py` | 203 | `txt_map = torch.load(cache_path, map_location=self.device)` | Training cache path loads torch artifacts without `weights_only=True`; unsafe if the cache file is untrusted. |
| `baselines/ultralytics/ultralytics/utils/patches.py` | 158 | `return torch.load(*args, **kwargs)` | Wrapper forwards arbitrary torch-load calls without enforcing `weights_only=True`; runtime safety depends on each caller. |
| `baselines/unseen_object_clustering/lib/fcn/config.py` | 448 | `yaml_cfg = edict(yaml.load(f))` | Config loading omits a safe/full YAML loader, so arbitrary object construction is possible from untrusted YAML. |
| `baselines/unseen_object_clustering/tools/test_images.py` | 196 | `network_data = torch.load(args.pretrained)` | Test image path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/unseen_object_clustering/tools/test_images.py` | 209 | `network_data_crop = torch.load(args.pretrained_crop)` | Test image crop path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/unseen_object_clustering/tools/test_net.py` | 110 | `network_data = torch.load(args.pretrained)` | Test net path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/unseen_object_clustering/tools/test_net.py` | 124 | `network_data_crop = torch.load(args.pretrained_crop)` | Test net crop path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/unseen_object_clustering/tools/test_npy.py` | 192 | `network_data = torch.load(args.pretrained)` | Test NPY path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/unseen_object_clustering/tools/test_npy.py` | 205 | `network_data_crop = torch.load(args.pretrained_crop)` | Test NPY crop path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/unseen_object_clustering/tools/train_net.py` | 115 | `network_data = torch.load(args.pretrained)` | Training path reads pretrained weights without `weights_only=True`; unsafe if the checkpoint path is untrusted. |
| `baselines/uoais/adet/checkpoint/adet_checkpoint.py` | 14 | `data = pickle.load(f, encoding="latin1")` | Checkpoint loading uses pickle directly; unsafe if checkpoint files are untrusted. |
| `baselines/uoais/eval/eval_utils.py` | 39 | `checkpoint = torch.load(os.path.join(args.cgnet_weight_path))` | Eval helper loads CG-Net weights without `weights_only=True`; unsafe if the weight path is untrusted. |
| `baselines/uoais/eval/eval_utils.py` | 182 | `checkpoint = torch.load(os.path.join(args.cgnet_weight_path))` | Eval helper loads CG-Net weights without `weights_only=True`; unsafe if the weight path is untrusted. |
| `baselines/uoais/eval/eval_utils.py` | 538 | `checkpoint = torch.load(os.path.join(args.cgnet_weight_path))` | Eval helper loads CG-Net weights without `weights_only=True`; unsafe if the weight path is untrusted. |
| `baselines/uoais/tools/k4a_demo.py` | 60 | `checkpoint = torch.load(os.path.join(args.cgnet_weight_path))` | Demo path loads CG-Net weights without `weights_only=True`; unsafe if the weight path is untrusted. |
| `baselines/uoais/tools/rs_demo.py` | 58 | `checkpoint = torch.load(os.path.join(args.cgnet_weight_path))` | Demo path loads CG-Net weights without `weights_only=True`; unsafe if the weight path is untrusted. |
| `baselines/uoais/tools/run_on_OSD.py` | 70 | `checkpoint = torch.load(os.path.join(args.cgnet_weight_path))` | Demo/eval path loads CG-Net weights without `weights_only=True`; unsafe if the weight path is untrusted. |
