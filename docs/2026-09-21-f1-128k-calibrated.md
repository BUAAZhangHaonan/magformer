# F1@128K 封存校准报告（CALIBRATED）— 2026-09-21 07:10

自动链：03:55 val@128000 → 停训 → 封存 `f1_seal_128k/`（SHA256 完整）→ 校准链重推理（evaluate.py 标准协议：全 val 3276、maxDets=100、topk 导出后按分数截断）。

## 一、封存终值（trainer in-training 口径）

| iter | segm mAP | AP_s | AP_m | AP_l | 备注 |
|---|---|---|---|---|---|
| 112K | 0.8692 | 0.2638 | 0.8620 | 0.9758 | |
| 120K | 0.8715 | 0.2675 | 0.8635 | 0.9759 | |
| **128K（终）** | **0.8744** | **0.2698** | — | — | vs c0 0.8603/0.2360：**+1.4 mAP / +3.4 AP_s** |

## 二、重推理校准（同协议配对，raw=last.pt@128K；EMA=best.pt@128K）

| 配置 | segm mAP | AP_s | AP_m | AP_l | ΔAP_s vs 基准 |
|---|---|---|---|---|---|
| raw @topk100/thr0.50（基准） | 0.8677 | 0.2598 | 0.8602 | 0.9758 | — |
| raw @topk200 | 0.8677 | 0.2607 | 0.8602 | 0.9767 | **+0.09** |
| raw @thr0.45 | 0.8678 | 0.2607 | 0.8596 | 0.9758 | +0.09（噪声内） |
| raw @thr0.40 | 0.8664 | 0.2604 | 0.8593 | 0.9749 | -0.13（略有害） |
| **EMA @topk100/thr0.50** | **0.8744** | **0.2700** | 0.8655 | 0.9769 | **+1.02** |

EMA 同协议复核与 trainer val（0.8744/0.2698）逐位吻合——评估链协议闭环验证。topk200 重跑经 `--truncate-to-max-dets`（按分数每图保留 top-100，COCO 语义等价，commit 4ebc733a）。原始逐场日志与 coco_metrics.json 在本目录各子目录。

## 三、校准结论（诚实版）

1. **F1@128K 校准终值 = 0.8744 / 0.2700**（EMA 权重）。对照 c0 终值 +1.4 mAP / +3.4 AP_s。
2. **EMA 比 raw 好 +0.67 mAP / +1.02 AP_s**——last.pt 存的是 raw 权重（与 best.pt 的 raw backup 827 张量 diff=0 逐位证实），部署/续训一律用 EMA（best.pt 的 model_state_dict）。方向与 R4 的"中程高 lr 噪声平台"判断一致。
3. **廉价解锁的两个推理侧杠杆实测近零**：topk200 仅 +0.09 AP_s（R1 依据 c0 的 +0.55pt 锚在 F1@128K 上不成立——keeper 已基本坐进 top-100）；二值化阈值 0.45/0.40 ≈ 0 或负。P4-d 场剩余价值集中在**重打分特征（CQTR 锚 +1.95~3.81）与 maskness 解耦**，topk/阈值分支按实测关闭。
4. **P1（AIM 去帽）GPU 验证**：6 单测全绿；41-GT（train p90）分块 3.34ms/调用（0.2% 迭代开销）——修复确认。
5. 掩码内容（27.1pt）与存在性+分数（16.5pt）两座大山不变——斗兽场 P3-a（BAS-CL+）与 P4-c（MAL-CP+）是主攻路径；本报告的负结果（推理侧无免费午餐）进一步收敛战场。

## 四、过程事故记录（如实）

- topk200 首跑被 evaluate.py 每图行数校验器拒绝（>maxDets）→ 加 `--truncate-to-max-dets` 修复（4ebc733a）；
- 重跑时一次重复启动撞卡导致 OOM（日志中 exit=1 为该死进程残留，存活进程完成全部推理）；
- raw-EMA 首轮对照无效（两次都加载了 raw）→ 发现 last.pt=raw 的事实并补跑 EMA 同协议复核。
